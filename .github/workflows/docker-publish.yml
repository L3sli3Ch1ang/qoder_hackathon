name: Publish Docker Image

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set lowercase owner
        run: echo "REPO_OWNER=l3sli3chiang" >> $GITHUB_ENV

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: qoder_cli
          file: qoder_cli/deploy/Dockerfile
          push: true
          tags: |
            ghcr.io/l3sli3chiang/qoder_hackathon:latest
            ghcr.io/l3sli3chiang/qoder_hackathon:${{ github.sha }}
